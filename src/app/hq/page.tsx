import { Page } from "@/components/withSession"; import { HqPanel } from "@/components/panels/Dashboards"; import { CompanyPanel } from "@/components/panels/Company";
export default async function P({ searchParams }: { searchParams: Promise<{ tab?: string }> }) { const { tab } = await searchParams; return <Page title="Công ty mẹ · đa trại — nhiều trại, nhiều vùng, nhiều pháp nhân">{(s) => <div className="space-y-3"><div className="flex gap-2"><a href="/hq" className={`px-4 py-2 rounded-xl font-semibold ${!tab ? "bg-brand-tok text-white" : "bg-white border"}`}>So sánh trại</a><a href="/hq?tab=cty" className={`px-4 py-2 rounded-xl font-semibold ${tab === "cty" ? "bg-brand-tok text-white" : "bg-white border"}`}>🏢 Quản trị công ty · Khai báo trại</a></div>{tab === "cty" ? <CompanyPanel sess={s} /> : <HqPanel />}</div>}</Page>; }
