import { Page } from "@/components/withSession"; import { HqPanel } from "@/components/panels/Dashboards"; import { CompanyPanel } from "@/components/panels/Company";
export default async function P({ searchParams }: { searchParams: Promise<{ tab?: string }> }) { const { tab } = await searchParams; return <Page title="Công ty mẹ · đa trại — nhiều trại, nhiều vùng, nhiều pháp nhân">{(s) => <div className="space-y-3"><div className="flex gap-2"><a href="/hq" aria-current={!tab ? "page" : undefined} className={!tab ? "tab-btn-active" : "tab-btn"}>So sánh trại</a><a href="/hq?tab=cty" aria-current={tab === "cty" ? "page" : undefined} className={tab === "cty" ? "tab-btn-active" : "tab-btn"}>🏢 Quản trị công ty · Khai báo trại</a></div>{tab === "cty" ? <CompanyPanel sess={s} /> : <HqPanel />}</div>}</Page>; }
