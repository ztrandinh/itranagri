import { Page } from "@/components/withSession"; import DesignSystem from "@/components/panels/DesignSystem";
export default function P() { return <Page title="Hệ thiết kế — token · primitive · trạng thái">{() => <DesignSystem />}</Page>; }
