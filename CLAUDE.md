# CLAUDE.md — ITRAN OS (Ray A · Next.js 16 + Postgres, chuẩn bị chạy trên Supabase)

## Dự án
Hệ điều hành số cho chuỗi trang trại tuần hoàn ITRAN FARM (công ty mẹ → vùng → nhiều trại F0x). Nghiệp vụ gốc: `docs/bo-goc/` (FILE-GỐC v3.1, Quyển 1–5, Phụ lục A/B). Kế hoạch & kiến trúc: `docs/plan/00…10`. Starter/góp ý ràng buộc: `docs/starter/`. **Bộ gốc thắng về nghiệp vụ; kế hoạch thắng về công nghệ; góp ý v1.5 thắng khi mâu thuẫn với kế hoạch.**

## 10 luật không được vi phạm
1. Mọi bảng nghiệp vụ có `farm_id` (+ ORG/REGION); RLS bật; ứng dụng kết nối bằng `app_user`, đặt ngữ cảnh qua `withCtx()` (`set_config app.*`).
2. Bảng sự kiện **append-only**: không UPDATE/DELETE (trigger DB + revoke); sửa = bản mới `supersedes_id`; ≤72h công nhân tự supersede, sau đó qua `adjustments` có duyệt.
3. Mã nghiệp vụ `[TRẠI]-[LOẠI]-[SỐ]` (`next_code()`); id kỹ thuật uuid; mọi POST sự kiện idempotent theo `client_ref`.
4. Định danh vật nuôi 3 cấp: cá thể (bò/dê bắt buộc) · lô nhập · đàn; bê chưa có tai vẫn tạo được (`tag_pending`), lịch sử thẻ ở `animal_tags`.
5. Offline-first: form ghi 3 chạm (`ThreeTap`) → hàng đợi IndexedDB (`lib/offline.ts`) → `/api/events/{table}` (207 per-item). Điện thoại công nhân = Android; giấy BM01–BM10 là luồng chính giai đoạn đầu (SPEC-02).
6. Quyền ghi ≠ quyền duyệt; không ai duyệt bản ghi của mình (`ERR_SELF_APPROVE`); chi >20tr cần 2 chữ ký, >50tr báo chủ.
7. Cấu hình = dữ liệu (`settings`, `norms`, `rc_rules`, `alert_rules`, `kpi_defs`, `price_list`) có phiên bản; cấm hằng số nghiệp vụ trong code.
8. Báo cáo = view/SQL (`v_*`, `lib/queries.ts`, `lib/metrics.ts`); thêm chỉ số = thêm 1 mục catalog, không sửa màn hình. Mọi số phải drill được về bản ghi gốc + người ghi.
9. Xuất dữ liệu mở: `/api/exports/{all|audit-pack|tt66|sales-tax|…}` CSV/ZIP+sha256/EPCIS 2.0; test quý: dựng lại từ Postgres trắng bằng `pnpm db:migrate`.
10. Không mở rộng phạm vi trong phiên; ý mới → `docs/backlog.md`. Connector chỉ xây khi có nghiệp vụ thật (docs/plan/05 §6b).

## Chạy
`docker start itranos_db` (PG 17.6, cổng 54499) · `pnpm db:migrate` · `pnpm db:seed:sim 30` (giả lập 30 ngày chi tiết) · `pnpm db:seed:history` (trại đã vận hành 30 tháng: 571 vật nuôi bò/dê/gà, 51 mùa vụ cỏ/bắp/lúa, 5.7k phiếu kho, 1.4k bán hàng, thời tiết 930 ngày, 65 nhân sự/17 phòng ban — reset & chạy lại được) · `pnpm dev --port 3111` · `pnpm test` (RLS/append-only/ngưng thuốc/RC) · `pnpm lint && pnpm exec tsc --noEmit`.
Job đêm: `POST /api/jobs/all?farm=F01` với header `x-job-key` (recon RC1–RC12 + alerts + tasks v2 + agg_daily/snapshot + feed_plans); `/api/jobs/agg?days=35` backfill. Trang: /trang-chu (khu vực) /ca /dan /canh-tac /kho /giay /gd /ktt /hq (+?tab=cty, /hq/trai/{id}) /audit /doi-soat /canh-bao (+?tab=caidat) /sop /ban-hang (+crm/pos/kenh) /du-lich /truy-xuat /suc-khoe /so-lieu (+?tab=any) /ke-hoach /thu-y /nhan-su /thiet-bi /ke-toan /to-chuc (phòng ban·quy trình·khai báo/chạy quy trình·I/O·event bus·hồ sơ) /doi-tuong /che-bien (KHSX·MRP·BOM·bao bì·nhãn·tem QR) /quan-tri (thêm/sửa/gỡ/nhập CSV/xuất/lịch sử mọi bảng) /xem/{type}/{id} (360 mọi đối tượng, ô 🔍 header) /tai-khoan /trace/{qr}. Job dispatch thông báo: `POST /api/jobs/dispatch?farm=F01` (x-job-key) mỗi phút hoặc trong /jobs/all. Tài khoản dev: owner/gd/ktt-cn/ks-cn/tn-bo/a1…a11/audit/kt + (sau seed:history) chutich/tgd/ktt-tc/kt-th/thuquy/tp-hcns/tp-qa/qc1/tp-kd/nvkd1/tp-ccu/muahang/tp-cn/ktt-ty/bsty/ktt-tt/cn-co1/cn-bap/cn-bo2/cn-de/cn-ga2/tn-sh/cn-trun/cn-d5-2/thukho/taixe/ktv-tb/tn-cb/letan/bep/hanhchinh… · PIN 1234. Menu = 16 nhóm theo phòng ban (docs/plan/17): Của tôi · Điều hành & Kế hoạch · Chăn nuôi–Thú y · Trồng trọt · Sinh học tuần hoàn (/sinh-hoc) · D5 & Chế biến · Chuỗi cung ứng · Kinh doanh · Du lịch · XNK · Tài chính · HCNS · Chất lượng–Tuân thủ · Công nghệ–Dữ liệu · R&D/Nhân rộng · Tổ chức–Danh mục. /sop → /to-chuc?tab=sop. Trang mới: /du-tru (dashboard dự trữ 3 khối), /mua-hang, /che-bien, /ke-hoach (Năm→S&OP 12 tháng→đàn theo lứa/lịch vụ→ban hành việc→KH–TT), /giam-sat (tiêu chí & điểm tuần), /nhan-su (đào tạo tuần & thưởng gắn lương). Rà soát mô-đun: docs/plan/16; biên bản đóng vai: docs/plan/15. Cache view nặng theo trại (cache_kv/cache_stock_dashboard, job cache 5', /jobs/maint tuần). Mọi trang: PageNav (← Quay lại · Phòng tôi · Tiếp →) + TodayBar "Hôm nay của tôi" (my_inbox: việc/duyệt/tin/đào tạo/giám sát/thay người) + ModuleIntro (để làm gì/cho ai/quy trình/SOP). Nghỉ phép có NGƯỜI THAY (0087: staff_delegations, activate_delegation chuyển việc, end_delegations trả về, expire_stale_tasks đóng việc vô chủ >14 ngày) — form ở /tai-khoan.

## Cấu trúc
`supabase/migrations/*.sql` (schema thuần SQL 0001–0024: 0009 quy mô/agg, 0012 kế hoạch/đơn/HĐ/thú y/nhận nuôi, 0014 P&L/KPI/portal, 0016 event_bus/notifications/create_farm, 0017 hồ sơ trại/facilities/audit_log/R&D/XNK/franchise/documents/webhooks, 0018 canh tác(PHI)/thu hoạch/CRM/POS/du lịch/records_catalog, 0020 phòng ban/quy trình/event_topics, 0021 đối tượng (roles/positions/species/classes/crops/product_kinds), 0022 kiểm kê 65 quy trình, 0023 workflow engine (khai báo/xuất bản/chạy quy trình), 0024 … 0042 thư viện SOP Quyển 3 §15 (81 L2 SOP-XX-NN / 422 L3 / 12 GN), 0043–0045 tầng tuân thủ SPEC-05 (controls, clause_controls, compliance_gaps, v_compliance_coverage, gen_compliance_tasks) — đối chiếu: docs/plan/14; 0046–0048 trồng trọt (weather_daily+ET0 FAO-56, crop_kc, irrigation_logs, pest_scouting, soil_tests, crop_rotation_plans, plot_contracts, v_water_balance); 0049–0051 chế biến (bom_headers/lines, mrp_explode/mrp_run, production_plans, packaging_specs, allergens, label_specs, v_lot_label); 0052–0054 kho (bins, v_bin_stock, suggest_putaway, stock_policies/v_replenishment, gen_cycle_counts, v_cold_chain, vehicles/trips/trip_stops, v_supplier_scorecard, v_stock_card); 0055–0057 bán hàng (customer_prices, discount_tiers, price_for, quotes/quote_to_order, sales_targets/v_sales_performance, loyalty_ledger, v_ar_aging/run_dunning, contract_deliveries); 0058–0061 tài chính (legal_entities/v_consolidated, v_ap_aging/pay_supplier, loans/loan_schedule/pay_loan_installment, approval_matrix/approval_limit, insurance_policies/claims, v_vat_summary, v_payment_calendar)) · `src/lib` (db, auth, events(zod), queries, metrics, anychart (biểu đồ MỌI trường), admin (sổ đăng ký bảng quản trị + CSV), forms, offline, jobs, notify (event bus → hộp thư, luật cấu hình, auto-start quy trình)) · `src/app/api` (auth, events, data, actions ~60, series (+/any), exports (+crop-dossier), jobs, upload, notifications, admin/[table], import/csv, search, public/trace) · `src/components/panels/*` (màn theo vai) · `scripts/` (migrate, simulate) · `tests/`.

## Chuẩn code
TS strict, không `any`; DB snake_case / TS camelCase; glossary `docs/starter/docs/glossary.md`; lỗi có mã `ERR_*`; UI tiếng Việt, code tiếng Anh; ≤3 chạm & ≤20s/bản ghi, chữ ≥16pt; test RLS bắt buộc khi thêm bảng; golden test khi thêm KPI/RC.
